<!DOCTYPE html>
<html>
<head>
  <title>Big Ruby Conf ActiveRecord Demonstration 2014</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header class="navbar navbar-default">
    <div class="container">
      <% if current_user %>
        <%= link_to "#{current_user.full_name}'s Address Book", user_contacts_path(current_user), class: "navbar-brand" %>
        <ul class="nav navbar-nav pull-right">
          <li><%= link_to "Edit Profile", edit_user_path(current_user) %></li>
          <li><%= link_to "Categories", user_categories_path(current_user) %></li>
          <li><%= link_to "Contacts", user_contacts_path(current_user) %></li>
        </ul>
      <% else %>
        <%= link_to "Address Book", root_url, class: "navbar-brand" %>
      <% end %>
    </div>
  </header>
  <div class="container">
    <%= flash[:notice] %>
    <%= flash[:error] %>
    <%= yield %>
  </div>
</body>
</html>
